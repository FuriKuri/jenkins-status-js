{
  "name": "jenkins-build-status",
  "version": "0.1.1",
  "description": "Small lib to get build status from the jenkins",
  "main": "lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Theo Pack",
  "license": "MIT",
  "dependencies": {
    "urlencode": "^0.2.0"
  }
}
